machine:
  node:
    version: 5.1.0

dependencies:
  cache_directories:
      - node_modules
  override:
    - npm install

test:
  override:
    - npm run test

deployment:
  production:
    branch: master
    commands:
      - git config --global user.name "Circle CI"
      - git config --global user.email "agrishun@me.com"
      - npm run build
      - cd dist && git init
      - cd dist && git add .
      - cd dist && git commit -m "Deploy to GitHub Pages [ci skip]"
      - cd dist && git push --force --quiet "https://github.com/infradash/dashboard.git" master:gh-pages
